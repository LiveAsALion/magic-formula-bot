name: Magic Formula Bot (DISABLED on main branch)

on:
  # Completely disabled — no schedule, no manual trigger
  workflow_dispatch:           # Optional: keeps the "Run workflow" button but does nothing
    if: false

jobs:
  run-bot:
    if: false                    # ← This line turns the whole job OFF
    runs-on: ubuntu-latest

    steps:
    - name: Disabled
      run: echo "Old bot workflow is disabled on main branch. Use the second branch instead."
